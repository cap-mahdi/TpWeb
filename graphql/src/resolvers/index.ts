import { CV } from "./cv";
import { Mutation } from "./mutation";
import { Query } from "./query";
import { User } from "./user";

const resolvers = {
  Query,
  Mutation,
  CV,
  User
};

export { resolvers };
